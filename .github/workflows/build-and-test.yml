name: build-and-test

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

env:
  PipelineEnvironment: true
  NUGET_XMLDOC_MODE: skip

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false

      - name: Setup .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 10.0.x

      - name: Restore tests
        run: dotnet restore test/Soenneker.Webflow.Runners.OpenApiClient.Tests/Soenneker.Webflow.Runners.OpenApiClient.Tests.csproj --verbosity minimal

      - name: Build tests
        run: dotnet build test/Soenneker.Webflow.Runners.OpenApiClient.Tests/Soenneker.Webflow.Runners.OpenApiClient.Tests.csproj --configuration Release --no-restore --verbosity minimal

      - name: Test
        run: dotnet test --project test/Soenneker.Webflow.Runners.OpenApiClient.Tests/Soenneker.Webflow.Runners.OpenApiClient.Tests.csproj --configuration Release --no-build --no-restore --verbosity minimal

      - name: Pack
        run: dotnet pack src/Soenneker.Webflow.Runners.OpenApiClient/Soenneker.Webflow.Runners.OpenApiClient.csproj --configuration Release --no-build --no-restore --output . --verbosity minimal
